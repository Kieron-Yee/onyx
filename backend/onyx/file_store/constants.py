"""
文件存储相关的常量定义模块

本模块包含了文件存储系统中使用的各种常量，主要定义了文件处理时的内存大小限制和分块大小等参数。
这些常量用于控制文件上传、下载和处理过程中的资源使用。
"""

# 内存中允许的最大文件大小限制
# 当文件大小超过此限制时，将使用磁盘临时存储
MAX_IN_MEMORY_SIZE = 30 * 1024 * 1024  # 30MB

# 文件处理时的标准分块大小
# 用于文件上传和下载时的数据分块传输
STANDARD_CHUNK_SIZE = 10 * 1024 * 1024  # 10MB chunks
